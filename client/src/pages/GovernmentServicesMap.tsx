import { MarkerClusterer } from "@googlemaps/markerclusterer";
import { MapView } from "@/components/Map";
import { Badge } from "@/components/ui/badge";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { supabase } from "@/lib/supabase";
import { cn } from "@/lib/utils";
import { govMapServices, provinces, serviceCategories, type GovMapService, type GovServiceLocation } from "@shared/govguide-map";
import {
  BriefcaseBusiness, Building2, Car, CheckCircle2, CircleAlert, Clock3, Compass, Crosshair, ExternalLink, Filter, Globe2,
  HeartPulse, Info, Landmark, List, Loader2, Map as MapIcon, MapPin, Navigation, RefreshCw, Search, ShieldCheck, SlidersHorizontal,
  SmartphoneNfc, Target, UserRound, X,
} from "lucide-react";
import type { LucideIcon } from "lucide-react";
import { useEffect, useMemo, useRef, useState } from "react";
import { toast } from "sonner";

const SOUTH_AFRICA_VIEW = { lat: -30.5595, lng: 22.9375 };
const serviceIcons: Record<GovMapService["icon"], LucideIcon> = { business: Building2, transport: Car, identity: SmartphoneNfc, social: HeartPulse, travel: Globe2, labour: BriefcaseBusiness };
function normalizeOfficeService(value: string) { return value.toLowerCase().replace(/[’']/g, "").replace(/[^a-z0-9]+/g, " ").trim(); }
function serviceIdsForOffice(values: unknown) {
  const names = Array.isArray(values) ? values.map(String) : [];
  return govMapServices.filter(service => names.some(name => { const normalized = normalizeOfficeService(name); const target = normalizeOfficeService(service.name); return normalized === target || normalized.includes(target) || target.includes(normalized) || (service.id === "business-registration" && normalized.includes("business")) || (service.id === "vehicle-licence-renewal" && normalized.includes("vehicle")); })).map(service => service.id);
}
function serviceMatchesQuery(query: string, service: GovMapService) {
  const normalized = query.toLowerCase();
  const aliases: Record<string, string[]> = { "business-registration": ["company", "register", "registration", "cipc", "business"], "driving-licence": ["driver", "driving", "licence test", "license test"], "learners-licence": ["learner", "learners", "learners licence"], "vehicle-licence-renewal": ["vehicle", "car licence", "car license", "renew", "disc"], "smart-id": ["smart id", "id card", "identity", "home affairs"], "social-grants": ["sassa", "grant", "social assistance", "social relief"], passport: ["passport", "travel"], "uif-benefits": ["uif", "employment", "labour", "unemployment"] };
  return normalized.includes(service.name.toLowerCase()) || normalized.includes(service.shortName.toLowerCase()) || aliases[service.id]?.some(alias => normalized.includes(alias));
}
function ServiceIcon({ service, className }: { service: GovMapService; className?: string }) { const Icon = serviceIcons[service.icon]; return <Icon className={className} aria-hidden="true" />; }
function ServiceCard({ service, selected, onSelect }: { service: GovMapService; selected: boolean; onSelect: () => void }) { return <button onClick={onSelect} className={cn("group flex w-full items-start gap-3 rounded-2xl border bg-white p-4 text-left transition hover:-translate-y-0.5 hover:border-[#46B5C2] hover:shadow-[0_10px_26px_rgba(7,59,76,.08)] dark:bg-[#0a4050]", selected ? "border-[#087E8B] bg-[#f1fbfb] shadow-[0_8px_22px_rgba(8,126,139,.12)] dark:border-[#46B5C2] dark:bg-[#073B4C]" : "border-[#dde7e9] dark:border-[#1f6570]")}><div className={cn("flex h-11 w-11 shrink-0 items-center justify-center rounded-xl transition", selected ? "bg-[#087E8B] text-white" : "bg-[#e4f4f5] text-[#087E8B] group-hover:bg-[#087E8B] group-hover:text-white")}><ServiceIcon service={service} className="h-5 w-5" /></div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><div className="font-[Manrope] text-sm font-extrabold text-[#073B4C] dark:text-white">{service.shortName}</div>{selected && <CheckCircle2 className="h-4 w-4 shrink-0 text-[#087E8B]" />}</div><div className="mt-1 text-xs leading-5 text-slate-500 dark:text-slate-300">{service.description}</div><div className="mt-2 text-[10px] font-bold text-[#087E8B]">{service.organisation} · {service.category}</div></div></button>; }
function distanceKm(from: { lat: number; lng: number }, to: Pick<GovServiceLocation, "latitude" | "longitude">) { if (to.latitude === undefined || to.longitude === undefined) return undefined; const toRad = (value: number) => value * Math.PI / 180; const dLat = toRad(to.latitude - from.lat); const dLng = toRad(to.longitude - from.lng); const a = Math.sin(dLat / 2) ** 2 + Math.cos(toRad(from.lat)) * Math.cos(toRad(to.latitude)) * Math.sin(dLng / 2) ** 2; return 6371 * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a)); }
function LocationCard({ location, service, userCoords, selected, onSelect, onDirections }: { location: GovServiceLocation; service: GovMapService; userCoords: { lat: number; lng: number } | null; selected: boolean; onSelect: () => void; onDirections: () => void }) { const distance = userCoords ? distanceKm(userCoords, location) : undefined; const hasCoordinates = location.latitude !== undefined && location.longitude !== undefined; return <div className={cn("rounded-2xl border bg-white p-4 transition dark:bg-[#0a4050]", selected ? "border-[#087E8B] shadow-[0_10px_26px_rgba(8,126,139,.1)]" : "border-[#dde7e9] dark:border-[#1f6570]")}><button onClick={onSelect} className="w-full text-left"><div className="flex items-start gap-3"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-[#e4f4f5] text-[#087E8B]"><ServiceIcon service={service} className="h-5 w-5" /></div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><div className="font-[Manrope] text-sm font-extrabold text-[#073B4C] dark:text-white">{location.name}</div><Badge className="shrink-0 bg-[#e7f4ec] text-[10px] text-[#2E8B57] hover:bg-[#e7f4ec]">Verified</Badge></div><div className="mt-1 text-xs font-semibold text-[#087E8B]">{location.organisation}</div><div className="mt-2 flex items-start gap-2 text-xs leading-5 text-slate-500 dark:text-slate-300"><MapPin className="mt-0.5 h-3.5 w-3.5 shrink-0 text-[#087E8B]" />{location.address ? `${location.address}, ` : ""}{location.city}, {location.province}</div><div className="mt-2 flex flex-wrap gap-1.5 text-[10px] font-semibold text-slate-400"><span className="rounded-md bg-[#f7fafa] px-2 py-1 dark:bg-[#073B4C]">{distance !== undefined ? `${distance.toFixed(1)} km away` : hasCoordinates ? "Distance unavailable" : "Map location pending"}</span><span className="rounded-md bg-[#f7fafa] px-2 py-1 dark:bg-[#073B4C]">{location.openingHours || "Hours unavailable"}</span></div></div></div></button><div className="mt-4 flex gap-2"><button onClick={onSelect} className="flex-1 rounded-xl border border-[#dde7e9] px-3 py-2 text-xs font-bold text-[#073B4C] hover:border-[#46B5C2] dark:border-[#2a6c76] dark:text-white">View details</button><button onClick={onDirections} disabled={!hasCoordinates} className="inline-flex items-center justify-center gap-1.5 rounded-xl bg-[#087E8B] px-3 py-2 text-xs font-bold text-white hover:bg-[#073B4C] disabled:cursor-not-allowed disabled:opacity-40"><Navigation className="h-3.5 w-3.5" /> Directions</button></div></div>; }
function LocationDetails({ location, service, userCoords, onDirections, onClose }: { location: GovServiceLocation; service: GovMapService; userCoords: { lat: number; lng: number } | null; onDirections: () => void; onClose: () => void }) { const distance = userCoords ? distanceKm(userCoords, location) : undefined; const hasCoordinates = location.latitude !== undefined && location.longitude !== undefined; return <div className="rounded-2xl border border-[#dde7e9] bg-white p-5 shadow-[0_10px_30px_rgba(7,59,76,.08)] dark:border-[#1f6570] dark:bg-[#0a4050]"><div className="flex items-start justify-between gap-4"><div><div className="text-[10px] font-bold uppercase tracking-widest text-[#087E8B]">Verified government office</div><h3 className="mt-1 font-[Manrope] text-lg font-extrabold text-[#073B4C] dark:text-white">{location.name}</h3></div><button onClick={onClose} aria-label="Close location details" className="rounded-lg p-1.5 text-slate-400 hover:bg-slate-100 dark:hover:bg-white/10"><X className="h-4 w-4" /></button></div><div className="mt-5 grid gap-3 text-xs text-slate-600 dark:text-slate-300 sm:grid-cols-2"><DetailRow icon={Building2} label="Organisation" value={location.organisation} /><DetailRow icon={Landmark} label="Department" value={location.department} /><DetailRow icon={MapPin} label="Address" value={`${location.address ?? "Address unavailable"}, ${location.city}, ${location.province}`} /><DetailRow icon={Compass} label="Distance" value={distance !== undefined ? `${distance.toFixed(1)} km from your location` : hasCoordinates ? "Enable location to calculate" : "Map location pending"} /><DetailRow icon={ShieldCheck} label="Verification" value={`Verified${location.lastVerified ? ` · ${location.lastVerified}` : ""}`} /></div><div className="mt-4 rounded-xl border border-[#f0df9d] bg-[#fff9e8] p-3 text-[11px] leading-5 text-[#735800] dark:border-[#6b5a1b] dark:bg-[#403711] dark:text-[#f7df80]">{location.dataNote}</div><div className="mt-4 flex flex-wrap gap-2"><button onClick={onDirections} disabled={!hasCoordinates} className="inline-flex items-center gap-2 rounded-xl bg-[#087E8B] px-4 py-2.5 text-xs font-bold text-white hover:bg-[#073B4C] disabled:cursor-not-allowed disabled:opacity-40"><Navigation className="h-3.5 w-3.5" /> Get directions</button>{location.website && <a href={location.website} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-xl border border-[#dde7e9] px-4 py-2.5 text-xs font-bold text-[#073B4C] hover:border-[#46B5C2] dark:border-[#2a6c76] dark:text-white"><ExternalLink className="h-3.5 w-3.5" /> Official source</a>}</div></div>; }
function DetailRow({ icon: Icon, label, value }: { icon: typeof MapPin; label: string; value: string }) { return <div className="flex gap-2"><Icon className="mt-0.5 h-4 w-4 shrink-0 text-[#087E8B]" /><div><div className="text-[10px] font-bold uppercase tracking-widest text-slate-400">{label}</div><div className="mt-1 font-semibold text-[#073B4C] dark:text-white">{value}</div></div></div>; }

export default function GovernmentServicesMap() {
  const [locationData, setLocationData] = useState<GovServiceLocation[]>([]); const [nearbyLocations, setNearbyLocations] = useState<GovServiceLocation[] | null>(null); const [dataLoading, setDataLoading] = useState(true); const [dataError, setDataError] = useState("");
  useEffect(() => { let active = true; (async () => { const [officesResult, departmentsResult] = await Promise.all([supabase.from("offices").select("id,department_id,name,address,city,province,postal_code,latitude,longitude,phone,email,opening_hours,services_supported,official_source_url,source_name,verification_status,verified_at,geocoding_status").eq("verification_status", "verified"), supabase.from("departments").select("id,name")]); if (!active) return; const firstError = officesResult.error || departmentsResult.error; if (firstError) { console.error("[Government offices]", firstError); setDataError("We couldn't load government office locations. Please try again."); setDataLoading(false); return; } const departments = new Map((departmentsResult.data ?? []).map(department => [String(department.id), department.name])); setLocationData((officesResult.data ?? []).map(office => { const department = departments.get(String(office.department_id)) ?? office.source_name ?? "Government office"; const hasCoordinates = office.latitude != null && office.longitude != null && Number.isFinite(Number(office.latitude)) && Number.isFinite(Number(office.longitude)); return { id: String(office.id), name: office.name, organisation: department, department, serviceIds: serviceIdsForOffice(office.services_supported), province: office.province ?? "", city: office.city ?? "", address: office.address ?? undefined, latitude: hasCoordinates ? Number(office.latitude) : undefined, longitude: hasCoordinates ? Number(office.longitude) : undefined, phone: office.phone ?? undefined, email: office.email ?? undefined, website: office.official_source_url ?? undefined, openingHours: office.opening_hours ? JSON.stringify(office.opening_hours) : undefined, verified: true, lastVerified: office.verified_at ? new Date(office.verified_at).toLocaleDateString() : undefined, dataNote: hasCoordinates ? "Verified government location." : "Verified address; map location pending coordinate verification." }; })); setDataLoading(false); })(); return () => { active = false; }; }, []);
  const organisations = useMemo(() => Array.from(new Set(locationData.map(location => location.organisation))).sort(), [locationData]); const [selectedServiceId, setSelectedServiceId] = useState<string | null>(null); const [query, setQuery] = useState(""); const [category, setCategory] = useState<(typeof serviceCategories)[number]>("All"); const [organisation, setOrganisation] = useState("All organisations"); const [province, setProvince] = useState("All provinces"); const [city, setCity] = useState(""); const [distanceLimit, setDistanceLimit] = useState("Any distance"); const [sort, setSort] = useState("Nearest"); const [userCoords, setUserCoords] = useState<{ lat: number; lng: number } | null>(null); const [locationError, setLocationError] = useState(""); const [locating, setLocating] = useState(false); const [selectedLocationId, setSelectedLocationId] = useState<string | null>(null); const [mobileView, setMobileView] = useState<"list" | "map">("list"); const [mapReady, setMapReady] = useState(false); const [mapError, setMapError] = useState(false); const mapRef = useRef<google.maps.Map | null>(null); const markersRef = useRef<any[]>([]); const clustererRef = useRef<MarkerClusterer | null>(null);
  const selectedService = govMapServices.find(service => service.id === selectedServiceId) ?? null; const queryMatch = useMemo(() => govMapServices.find(service => serviceMatchesQuery(query, service)), [query]); const filteredLocations = useMemo(() => { const source = nearbyLocations ?? locationData; const maxDistance = distanceLimit === "Any distance" ? Infinity : Number(distanceLimit.replace(" km", "")); const locations = source.filter(location => { const matchesService = !selectedServiceId || location.serviceIds.includes(selectedServiceId); const matchesCategory = category === "All" || (selectedServiceId ? selectedService?.category === category : govMapServices.some(service => service.category === category && location.serviceIds.includes(service.id))); const matchesOrganisation = organisation === "All organisations" || location.organisation === organisation; const matchesProvince = province === "All provinces" || location.province === province; const haystack = [location.name, location.organisation, location.department, location.address, location.city, location.province].filter(Boolean).join(" ").toLowerCase(); const matchesSearch = !query.trim() || haystack.includes(query.trim().toLowerCase()) || Boolean(queryMatch && location.serviceIds.includes(queryMatch.id)); const matchesCity = !city.trim() || `${location.city} ${location.province} ${location.address ?? ""}`.toLowerCase().includes(city.trim().toLowerCase()); const distance = userCoords ? distanceKm(userCoords, location) : undefined; const matchesDistance = distanceLimit === "Any distance" || (distance !== undefined && distance <= maxDistance); return matchesService && matchesCategory && matchesOrganisation && matchesProvince && matchesSearch && matchesCity && matchesDistance; }).map(location => ({ location, distance: userCoords ? distanceKm(userCoords, location) : undefined })); if (sort === "Alphabetical") locations.sort((a, b) => a.location.name.localeCompare(b.location.name)); else if (sort === "Most relevant") locations.sort((a, b) => Number(b.location.verified) - Number(a.location.verified) || (a.distance ?? Infinity) - (b.distance ?? Infinity)); else locations.sort((a, b) => (a.distance ?? Infinity) - (b.distance ?? Infinity)); return locations.map(item => item.location); }, [locationData, nearbyLocations, selectedServiceId, selectedService, category, organisation, province, city, distanceLimit, sort, userCoords, query, queryMatch]);
  const selectedLocation = filteredLocations.find(location => location.id === selectedLocationId) ?? null;
  useEffect(() => { if (queryMatch && query.trim().length > 2) setSelectedServiceId(queryMatch.id); }, [queryMatch, query]); useEffect(() => { if (!selectedLocationId || filteredLocations.some(location => location.id === selectedLocationId)) return; setSelectedLocationId(filteredLocations[0]?.id ?? null); }, [filteredLocations, selectedLocationId]); useEffect(() => { const timer = window.setTimeout(() => { if (!mapReady) setMapError(true); }, 7000); return () => window.clearTimeout(timer); }, [mapReady]);
  useEffect(() => { const map = mapRef.current; if (!map || !mapReady) return; clustererRef.current?.clearMarkers(); markersRef.current.forEach(marker => { marker.map = null; }); markersRef.current = []; filteredLocations.forEach(location => { if (location.latitude === undefined || location.longitude === undefined || !window.google?.maps?.marker) return; const service = govMapServices.find(candidate => location.serviceIds.includes(candidate.id)); if (!service) return; const marker = new window.google.maps.marker.AdvancedMarkerElement({ map, position: { lat: location.latitude, lng: location.longitude }, title: `${location.name} — ${service.shortName}` }); marker.addListener("click", () => { setSelectedLocationId(location.id); setMobileView("map"); }); markersRef.current.push(marker); }); clustererRef.current = new MarkerClusterer({ map, markers: markersRef.current }); const mappable = filteredLocations.filter(location => location.latitude !== undefined && location.longitude !== undefined); if (mappable.length > 1) { const bounds = new google.maps.LatLngBounds(); mappable.forEach(location => bounds.extend({ lat: location.latitude as number, lng: location.longitude as number })); map.fitBounds(bounds, 64); } else if (mappable.length === 1) map.panTo({ lat: mappable[0].latitude as number, lng: mappable[0].longitude as number }); return () => { clustererRef.current?.clearMarkers(); clustererRef.current = null; markersRef.current.forEach(marker => { marker.map = null; }); markersRef.current = []; }; }, [filteredLocations, mapReady]);
  useEffect(() => { const map = mapRef.current; if (!map || !selectedLocation?.latitude || !selectedLocation?.longitude) return; map.panTo({ lat: selectedLocation.latitude, lng: selectedLocation.longitude }); if ((map.getZoom() ?? 0) < 9) map.setZoom(11); }, [selectedLocation]);
  const selectService = (service: GovMapService) => { setNearbyLocations(null); setSelectedServiceId(service.id); setCategory(service.category); setSelectedLocationId(null); setMobileView("list"); }; const detectLocation = () => { setLocationError(""); if (!navigator.geolocation) { setLocationError("Location access is required to find services near you."); return; } setLocating(true); navigator.geolocation.getCurrentPosition(async position => { const next = { lat: position.coords.latitude, lng: position.coords.longitude }; setUserCoords(next); const fetchNearby = async (radius: number) => supabase.rpc("find_nearby_offices", { user_lat: next.lat, user_lng: next.lng, radius_km: radius, service_name: selectedService?.name ?? null, department_name: organisation === "All organisations" ? null : organisation, province_name: province === "All provinces" ? null : province, search_query: query.trim() || null }); let response = await fetchNearby(25); let radius = 25; if (!response.error && !(response.data?.length ?? 0)) { radius = 50; response = await fetchNearby(50); } if (!response.error && !(response.data?.length ?? 0)) { radius = 100; response = await fetchNearby(100); } if (response.error) { console.error("[Nearby offices]", response.error); setLocationError("We couldn't load government office locations. Please try again."); setLocating(false); return; } const results = (response.data ?? []).map((office: any) => ({ id: String(office.id), name: office.name, organisation: office.department_name ?? "Government office", department: office.department_name ?? "Government office", serviceIds: serviceIdsForOffice(office.services), province: office.province ?? "", city: office.city ?? "", address: office.address ?? undefined, latitude: Number(office.latitude), longitude: Number(office.longitude), phone: office.phone ?? undefined, email: office.email ?? undefined, website: office.source_url ?? undefined, openingHours: office.opening_hours ? JSON.stringify(office.opening_hours) : undefined, verified: office.verification_status === "verified", lastVerified: office.verified_at ? new Date(office.verified_at).toLocaleDateString() : undefined, dataNote: `Verified government location · ${(Number(office.distance_km) || 0).toFixed(1)} km away` })); setNearbyLocations(results); setDistanceLimit(`${radius} km`); setLocating(false); if (!results.length) setLocationError(`No verified offices were found within ${radius} km. Search by city or province instead.`); else toast("Government offices found", { description: `${results.length} verified office${results.length === 1 ? "" : "s"} within ${radius} km.` }); }, () => { setLocating(false); setLocationError("Location access is required to find services near you."); }); }; const getDirections = (location: GovServiceLocation) => { if (location.latitude === undefined || location.longitude === undefined) { toast("Directions unavailable", { description: "This location does not have verified coordinates yet." }); return; } const destination = `${location.latitude},${location.longitude}`; const origin = userCoords ? `&origin=${userCoords.lat},${userCoords.lng}` : ""; window.open(`https://www.google.com/maps/dir/?api=1&destination=${destination}${origin}`, "_blank", "noopener,noreferrer"); }; const resetFilters = () => { setNearbyLocations(null); setSelectedServiceId(null); setQuery(""); setCategory("All"); setOrganisation("All organisations"); setProvince("All provinces"); setCity(""); setDistanceLimit("Any distance"); setSort("Nearest"); };
  return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div><div className="mb-2 text-[11px] font-bold uppercase tracking-[0.18em] text-[#087E8B]">Government services map</div><h1 className="font-[Manrope] text-3xl font-extrabold tracking-tight text-[#073B4C] dark:text-white">Find the right place to take action.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500 dark:text-slate-300">Search verified government offices from the Supabase directory, with or without map coordinates.</p></div><button onClick={detectLocation} disabled={locating} className="inline-flex items-center justify-center gap-2 rounded-xl bg-[#087E8B] px-4 py-3 text-xs font-bold text-white shadow-[0_8px_20px_rgba(8,126,139,.18)] hover:bg-[#073B4C] disabled:opacity-60"><Crosshair className="h-4 w-4" />{locating ? "Finding you..." : "Find services near me"}</button></div>{dataLoading && <div className="rounded-xl border border-[#b9dfe0] bg-[#f7fafa] px-4 py-3 text-xs text-[#087E8B]">Loading government offices...</div>}{dataError && <div className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-xs leading-5 text-red-700">{dataError}</div>}{locationError && <div className="flex items-start gap-2 rounded-xl border border-[#f0df9d] bg-[#fff9e8] px-4 py-3 text-xs leading-5 text-[#735800] dark:border-[#6b5a1b] dark:bg-[#403711] dark:text-[#f7df80]"><CircleAlert className="mt-0.5 h-4 w-4 shrink-0" />{locationError} <span className="font-semibold">Search by city or province instead.</span></div>}<div className="rounded-2xl border border-[#dde7e9] bg-white p-4 shadow-[0_8px_28px_rgba(7,59,76,.035)] dark:border-[#1f6570] dark:bg-[#0a4050]"><div className="flex flex-col gap-3 lg:flex-row"><div className="flex flex-1 items-center gap-3 rounded-xl border border-[#dde7e9] px-3 dark:border-[#2a6c76]"><Search className="h-4 w-4 text-slate-400" /><input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search office, department, service, city, province, or address..." className="w-full bg-transparent py-3 text-sm outline-none" aria-label="Search government offices" /></div><button onClick={() => queryMatch && selectService(queryMatch)} disabled={!queryMatch} className="rounded-xl bg-[#073B4C] px-4 py-3 text-xs font-bold text-white hover:bg-[#087E8B] disabled:cursor-not-allowed disabled:opacity-45">Filter service</button></div></div><div className="grid gap-5 lg:grid-cols-[360px_1fr]"><section className="space-y-4"><div className="flex items-center justify-between"><div><div className="text-[10px] font-bold uppercase tracking-widest text-[#087E8B]">Services</div><h2 className="mt-1 font-[Manrope] text-lg font-extrabold text-[#073B4C] dark:text-white">Filter by service</h2></div><span className="rounded-full bg-[#e4f4f5] px-2.5 py-1 text-[10px] font-bold text-[#087E8B]">{govMapServices.length} services</span></div><div className="space-y-2">{govMapServices.map(service => <ServiceCard key={service.id} service={service} selected={selectedServiceId === service.id} onSelect={() => selectService(service)} />)}</div></section><section className="min-w-0 space-y-4"><div className="relative z-30 grid gap-2 sm:grid-cols-2 lg:grid-cols-4"><div className="min-w-0"><Select value={category} onValueChange={value => setCategory(value as typeof category)}><SelectTrigger className="h-10 w-full min-w-0 rounded-xl border-[#dde7e9] bg-white text-xs dark:border-[#2a6c76] dark:bg-[#0a4050]"><Filter className="mr-2 h-3.5 w-3.5 text-[#087E8B]" /><SelectValue /></SelectTrigger><SelectContent className="z-[100] w-[var(--radix-select-trigger-width)] min-w-[12rem]" align="start">{serviceCategories.map(item => <SelectItem value={item} key={item}>{item}</SelectItem>)}</SelectContent></Select></div><div className="min-w-0"><Select value={organisation} onValueChange={setOrganisation}><SelectTrigger className="h-10 w-full min-w-0 rounded-xl border-[#dde7e9] bg-white text-xs dark:border-[#2a6c76] dark:bg-[#0a4050]"><Building2 className="mr-2 h-3.5 w-3.5 text-[#087E8B]" /><SelectValue /></SelectTrigger><SelectContent><SelectItem value="All organisations">All organisations</SelectItem>{organisations.map(item => <SelectItem value={item} key={item}>{item}</SelectItem>)}</SelectContent></Select></div><div className="min-w-0"><Select value={province} onValueChange={setProvince}><SelectTrigger className="h-10 w-full min-w-0 rounded-xl border-[#dde7e9] bg-white text-xs dark:border-[#2a6c76] dark:bg-[#0a4050]"><MapPin className="mr-2 h-3.5 w-3.5 text-[#087E8B]" /><SelectValue /></SelectTrigger><SelectContent><SelectItem value="All provinces">All provinces</SelectItem>{provinces.map(item => <SelectItem value={item} key={item}>{item}</SelectItem>)}</SelectContent></Select></div><div className="min-w-0"><div className="flex h-10 items-center rounded-xl border border-[#dde7e9] bg-white px-3 dark:border-[#2a6c76] dark:bg-[#0a4050]"><Search className="mr-2 h-3.5 w-3.5 text-[#087E8B]" /><input value={city} onChange={event => setCity(event.target.value)} placeholder="City or province" aria-label="Search city or province" className="w-full bg-transparent text-xs outline-none" /></div></div><div className="flex flex-wrap items-center justify-between gap-2"><div className="text-xs text-slate-400">{dataLoading ? "Loading government offices..." : `${filteredLocations.length} verified offices${userCoords ? " near you" : " in the verified Supabase dataset"}`}</div><div className="flex items-center gap-2"><Select value={sort} onValueChange={setSort}><SelectTrigger className="h-8 w-[145px] rounded-lg border-[#dde7e9] bg-white text-[11px] dark:border-[#2a6c76] dark:bg-[#0a4050]"><SlidersHorizontal className="mr-1.5 h-3 w-3 text-[#087E8B]" /><SelectValue /></SelectTrigger><SelectContent>{["Nearest", "Most relevant", "Alphabetical"].map(item => <SelectItem value={item} key={item}>{item}</SelectItem>)}</SelectContent></Select><button onClick={resetFilters} className="rounded-lg p-2 text-slate-400 hover:bg-[#e4f4f5] hover:text-[#087E8B]" aria-label="Reset map filters"><RefreshCw className="h-3.5 w-3.5" /></button></div></div></div><div className="grid gap-4 lg:grid-cols-[.88fr_1.12fr]"><div className={cn("space-y-3", mobileView === "map" && "hidden lg:block")}>{filteredLocations.length ? filteredLocations.map(location => <LocationCard key={location.id} location={location} service={selectedService ?? govMapServices.find(service => location.serviceIds.includes(service.id)) ?? govMapServices[0]} userCoords={userCoords} selected={selectedLocationId === location.id} onSelect={() => setSelectedLocationId(location.id)} onDirections={() => getDirections(location)} />) : <div className="rounded-2xl border border-dashed border-[#b9dfe0] bg-white p-7 text-center dark:bg-[#0a4050]"><MapPin className="mx-auto h-8 w-8 text-[#46B5C2]" /><div className="mt-3 text-sm font-bold text-[#073B4C] dark:text-white">{dataLoading ? "Loading government offices..." : "No verified offices match your selected filters."}</div><p className="mt-2 text-xs leading-5 text-slate-500">{dataLoading ? "Please wait while the verified Supabase directory loads." : "Try another province, city, service, or expand the search radius."}</p><div className="mt-4 flex justify-center gap-2"><button onClick={() => setDistanceLimit("Any distance")} className="rounded-lg bg-[#087E8B] px-3 py-2 text-[11px] font-bold text-white">Show all distances</button><button onClick={() => setProvince("All provinces")} className="rounded-lg border border-[#dde7e9] px-3 py-2 text-[11px] font-bold text-[#073B4C] dark:border-[#2a6c76] dark:text-white">Clear province</button></div></div>}</div><div className={cn("space-y-3", mobileView === "list" && "hidden lg:block")}><div className="relative min-h-[520px] overflow-hidden rounded-2xl border border-[#bddbd9] bg-[#e8f3f1] dark:border-[#1f6570] dark:bg-[#073B4C]"><div className="absolute left-3 top-3 z-10 flex items-center gap-2 rounded-xl bg-white/90 px-3 py-2 text-[11px] font-bold text-[#073B4C] shadow-sm backdrop-blur dark:bg-[#073B4C]/90 dark:text-white"><MapIcon className="h-3.5 w-3.5 text-[#087E8B]" />Verified office coverage</div>{!mapReady && !mapError && <div className="absolute inset-0 z-20 flex items-center justify-center bg-[#e8f3f1]/85 dark:bg-[#073B4C]/85"><div className="text-center"><Loader2 className="mx-auto h-7 w-7 animate-spin text-[#087E8B]" /><div className="mt-3 text-xs font-bold text-[#073B4C] dark:text-white">Loading map...</div></div></div>}{mapError && !mapReady && <div className="absolute inset-0 z-20 flex items-center justify-center bg-[#e8f3f1] p-6 text-center dark:bg-[#073B4C]"><div><CircleAlert className="mx-auto h-8 w-8 text-[#C94C4C]" /><div className="mt-3 text-sm font-bold text-[#073B4C] dark:text-white">Map temporarily unavailable.</div><p className="mt-2 text-xs leading-5 text-slate-500 dark:text-slate-300">You can still view verified offices in list format.</p></div></div>}<MapView className="h-[520px]" initialCenter={SOUTH_AFRICA_VIEW} initialZoom={5} onMapReady={map => { mapRef.current = map; setMapReady(true); setMapError(false); }} />{userCoords && <div className="absolute bottom-3 left-3 z-10 flex items-center gap-2 rounded-xl bg-white/90 px-3 py-2 text-[11px] font-bold text-[#073B4C] shadow-sm backdrop-blur dark:bg-[#073B4C]/90 dark:text-white"><UserRound className="h-3.5 w-3.5 text-[#087E8B]" />Distances are calculated from your browser location</div>}</div>{selectedLocation && <LocationDetails location={selectedLocation} service={selectedService ?? govMapServices.find(service => selectedLocation.serviceIds.includes(service.id)) ?? govMapServices[0]} userCoords={userCoords} onDirections={() => getDirections(selectedLocation)} onClose={() => setSelectedLocationId(null)} />}</div></div></section></div><div className="flex items-start gap-3 rounded-2xl border border-[#f0df9d] bg-[#fff9e8] p-4 text-xs leading-6 text-[#735800] dark:border-[#6b5a1b] dark:bg-[#403711] dark:text-[#f7df80]"><ShieldCheck className="mt-1 h-4 w-4 shrink-0" /><span>Verified offices come from Supabase. Offices without coordinates remain searchable in the list and are labelled Map location pending; confirm address, hours, and service availability with the responsible organisation.</span></div></div>;
}
